class Supplier {
    constructor(name, email, phone) {
        
        this.name = name; /* Armazena o nome do fornecedor. */
        this.email = email; /* Armazena o email do fornecedor. */
        this.phone = phone; /* Armazena o telefone do fornecedor. */
        
    }
}

module.exports = Supplier;